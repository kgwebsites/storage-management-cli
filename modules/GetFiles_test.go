package storagemanagementcli

import (
	"testing"
)

func TestGetFiles(t *testing.T) {
	// expectedParam1 := ""
	// expectedParam2 := ""

	// walkDir = func(param1 string, param2 string) {
	// 	expectedParam1 = param1
	// 	expectedParam2 = param2
	// }

	// GetFiles("a", "b")
	// if expectedParam1 != "a" || expectedParam2 != "b" {
	// 	t.Errorf("expected GetFiles to pass the root and ignore strings through to walkDir(), but it passed %s and %s", expectedParam1, expectedParam2)
	// }
}
